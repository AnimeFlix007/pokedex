export interface User {
  displayName: string;
  email: string;
  photoURL: string;
  id: string;
}

export interface Pokemon {
  name: string;
  url: string;
}

export interface PokemonDetail {
  name: string;
}